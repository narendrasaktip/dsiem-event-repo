# =========================================================
# KONFIGURASI GITHUB
# =========================================================
# Repositori tempat semua konfigurasi plugin SIEM disimpan.
GITHUB_REPO="narendrasaktip/siem-event-name-repository"

# Personal Access Token (PAT) GitHub untuk otentikasi API.
# GANTI DENGAN TOKEN ANDA. JANGAN COMMIT TOKEN INI KE REPOSITORI.
GITHUB_TOKEN="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"

# Branch default yang akan digunakan untuk operasi pull/push.
GITHUB_BRANCH="main"

# =========================================================
# KONFIGURASI ELASTICSEARCH / OPENSEARCH
# =========================================================
# Alamat host dan port dari cluster Elasticsearch/OpenSearch.
ES_HOST="http://opensearch:9200"

# Path absolut ke file yang berisi kredensial (username:password).
ES_PASSWD_FILE="/root/.passwd/es_passwd"

# Username yang akan dicari di dalam file ES_PASSWD_FILE.
ES_USER_LOOKUP="systemadm"

# Set ke "true" jika cluster ES menggunakan sertifikat TLS yang valid.
VERIFY_TLS="false"

# Waktu timeout untuk query ke ES dalam milidetik.
TIMEOUT="3000"

# =========================================================
# KONFIGURASI PERILAKU SKRIP (main.py)
# =========================================================
# Path ke template Logstash 70-plugin.conf.
TEMPLATE_PATH="./template-70.js"

# Path ke template Vector .yaml.
VECTOR_TEMPLATE_PATH="./template-vector.js"

# Direktori output untuk file-file yang di-generate (cth: .tsv, .conf).
OUT_DIR="."

# Path untuk menyimpan file metadata sementara (build_meta.json).
META_PATH="./build_meta.json"

# Mode otomatis untuk 'main.py', jika "1" tidak akan menanyakan input berulang.
AUTO_USE_CONFIG="0"

# =========================================================
# KONFIGURASI PLUGIN & DATA
# =========================================================
# Angka awal untuk penomoran plugin_sid jika membuat kamus baru.
PLUGIN_SID_START="1"

# Interval refresh kamus (translate) di Logstash dalam detik.
DICT_REFRESH_INTERVAL="60"

# =========================================================
# KONFIGURASI EMAIL NOTIFIKASI (SUDAH DISESUAIKAN)
# =========================================================
# Server SMTP untuk mengirim email.
EMAIL_SMTP_SERVER="smtp.gmail.com"

# Port server SMTP.
EMAIL_SMTP_PORT="587"

# Alamat email yang akan digunakan sebagai pengirim.
EMAIL_SENDER="narendra.prabawa@defenxor.com"

# App Password yang di-generate dari akun Google (JANGAN GUNAKAN PASSWORD BIASA).
EMAIL_APP_PASSWORD="okmx ulyc ynym vfln"

# Daftar email penerima, dipisahkan dengan koma, TANPA spasi.
EMAIL_RECIPIENTS="narendra.prabawa@defenxor.com,jose.alnevo@defenxor.com,muhammad.madani@defenxor.com,andi.wahyudi@defenxor.com"
